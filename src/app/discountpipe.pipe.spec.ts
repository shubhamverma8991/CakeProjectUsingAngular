import { DiscountpipePipe } from './discountpipe.pipe';

describe('DiscountpipePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountpipePipe();
    expect(pipe).toBeTruthy();
  });
});
